create image.img --size 20MiB
use -i image.img /dev/a
use
gpt init /dev/a 
gpt add  /dev/a --type linuxfs --name "rootfs" --start 1MiB --size 100%
gpt print /dev/a
mkfs.ext2 /dev/a --part 1 --label rootfs
mount /dev/a1 /
mount
pwd
ls -la
use -i disc.iso /dev/b
mkdir /mnt
mount /dev/b /mnt
cp /mnt/hello.txt /hello.txt
